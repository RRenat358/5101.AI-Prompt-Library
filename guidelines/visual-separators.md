# Система визуальных разделителей

Настоящий документ определяет правила применения визуальных разделителей
в промптах, сниппетах и сопроводительных файлах репозитория.
Разделители помогают человеку быстро ориентироваться в структуре,
а LLM — чётче разграничивать смысловые блоки.

## 1. Базовые принципы

1. **Вес символа** — чем «тяжелее» символ, тем выше уровень разделения:
   `━━━` > `===` > `---` (используется особым образом).

2. **Длина строки** — чем короче линия, тем меньше её разделительная сила.
   Принята строгая градация длин: **60, 40, 20, 10, 5, 3** символов.

3. **Единообразие** — внутри одного промпта используйте не более 3–4 типов
   разделителей, соблюдая их иерархию.

4. **Не перегружайте** — избыток линий размывает внимание.
   Разделитель ставится только перед началом логического блока.

## 2. Стандартные разделители и их назначение

| Символ  | Длина | Уровень      | Когда использовать |
|:--------|:------|:-------------| :--- |
| `━━━`   | 30    | Максимальный | Обрамление мета-блока. Самая важная граница — между метой и телом промпта. |
| `=====` | 60    | Высокий      | Разделение крупных частей внутри тела: Роль, Контекст, Правила, Формат ответа. |
| `=====` | 30    | Высокий      | Разделение подсекций внутри крупных частей. |
| `-----` | 20    | Средний      | Разделение подсекций внутри одной крупной части (например, разные группы правил). |
| `=====` | 10    | Умеренный    | Разделение мелких подразделов или важных акцентов внутри секции. |
| `-----` | 5     | Низкий       | Отбивка небольших уточнений, примеров, примечаний. |
| `---`   | 3     | Особый       | Только для отделения мета-блока (если не используется `━━━`), финального вывода, дополнительных системных установок, не входящих в тело промпта. Никогда не применяется внутри блоков. |

## 3. Особый случай `---` (3 символа)

Строка из трёх дефисов `---` — стандартный разделитель Markdown.
В нашем проекте он зарезервирован для трёх ситуаций:

- отделение мета-блока от тела промпта (если не используется `━━━`);
- выделение финального ответа или резюме в конце промпта;
- отделение дополнительных системных инструкций, не входящих в основной промпт.

Никогда не вставляйте `---` как рядовой разделитель внутри блоков — для этого есть линии 5, 10 или 20 символов.

## 4. Почему именно эти символы и длины

Выбранные символы (`━`, `=`, `-`) образуют естественную визуальную иерархию
от «тяжёлого» к «лёгкому» и не конфликтуют с синтаксисом распространённых форматов (Markdown, AsciiDoc).
Жёсткая сетка длин (60–30–20–10–5–3) внедрена по следующим причинам:

- **Предсказуемость.** Автору не нужно каждый раз подбирать длину — она уже задана.
- **Масштабирование.** При копировании в разные редакторы и среды (IDE, блокнот, GitHub) линии сохраняют пропорции и не ломаются.
- **Узнаваемость.** По длине и символу можно мгновенно определить уровень вложенности, даже не вчитываясь в заголовок.

## 5. Заготовки для копирования

Ниже приведены готовые строки разделителей всех уровней.

""""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ (30)
============================================================ (60)
============================== (30)
-------------------- (20)
========== (10)
----- (5)
--- (3)
""""

## 6. Примеры использования

Подробные иллюстрации с комментариями и указанием уровней вложенности
вынесены в отдельную папку `guidelines/examples/`. Начните с файла
`separators-basic.txt` — он демонстрирует простой и сложный промпт
с корректным применением всех разделителей.

## 7. Связь с другими гайдами

- Мета-блок и теги описаны в `AGENTS.md` и `guidelines/tagging-system.md`.
- Правила оформления заголовков блоков будут вынесены в отдельный гайд (в разработке).
- Общий стиль написания промптов — в `guidelines/style-guide.md`.

